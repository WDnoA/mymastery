# mastery - Android Studio 使用Flutter框架通过trae开发的个人资产管理手机App

## 项目概述

一款基于 **Flutter** 框架开发的个人资产管理应用，帮助用户记录和管理购买的电子产品、家具、车辆等资产，计算日均使用成本，直观了解资产价值分布。

## 技术栈

| 类别       | 技术选型                      | 说明                       |
| ---------- | ----------------------------- | -------------------------- |
| 框架       | Flutter 3.x                   | Dart 语言，跨平台UI框架    |
| 状态管理   | Riverpod 2.x                  | 响应式、类型安全的状态管理 |
| 数据库     | drift (SQLite)                | 类型安全的SQLite ORM       |
| 路由       | go_router                     | 声明式路由管理             |
| UI         | Material Design 3             | Flutter 原生 M3 支持       |
| 主题色     | 草绿色 (#4CAF50)              | 清新、资产感               |
| 最低SDK    | Android API 31 (Android 12.0) | 目标平台                   |
| 认证       | SHA-256                       | 密码登录                   |
| 安全存储   | flutter_secure_storage        | 密码哈希安全存储           |
| 设置持久化 | shared_preferences            | 设置项本地持久化           |

### 平台支持

| 平台    | 状态      | 说明                              |
| ------- | --------- | --------------------------------- |
| Android | ✅ 已完成 | API 31+，签名打包，混淆与资源压缩 |
| Web     | ✅ 已完成 | WASM + Drift，需跨域隔离头        |
| iOS     | ❌ 不支持 | 不考虑                            |
| Windows | ⏳ 待开发 | 后续进行桌面端开发                |

## 功能模块

### 1. 资产总览（首页）

- 统计卡片：总资产价值、日均使用成本、服役中数量、已退役/已卖出数量
- 最近添加的5条资产记录
- 空状态提示 + 加载示例数据按钮

### 2. 统计图表

- 资产统计概览：总价值、日均成本、资产总数
- 状态分布：服役中/已退役/已卖出 占比进度条
- 价值排行：按价格降序排列 Top 10
- 空状态提示

### 3. 全部资产

- 资产列表展示（图标、名称、价格、状态、使用天数、日均成本）
- 支持千位分隔符、时长显示格式切换（天数/日期）
- 点击资产进入编辑
- 滑动或长按删除（带确认弹窗）
- 空状态提示 + 加载示例数据按钮

### 4. 添加/编辑资产

- 选择图标（emoji列表）
- 输入资产名称
- 输入购买价格
- 选择购买日期（日期选择器）
- 选择状态：服役中 / 已退役 / 已卖出
- 选择资产分类
- 表单校验

### 5. 设置

- 显示与外观：主题模式切换（跟随系统/亮色/暗色）
- 安全与隐私：修改密码、切换账号（多账号数据隔离）、忘记密码说明
- 数值与单位：货币单位选择、小数点位数、千位分隔符开关、时长显示格式（天数/日期）
- 数据管理：分类管理、备份与恢复（含 CSV 导出分享）
- 关于：版本信息（动态读取）、隐私政策、用户协议

### 6. 登录与认证

- 首次启动设置密码（至少4位，SHA-256加密存储）
- 密码登录验证
- 登录界面：渐变背景、动画Logo、圆角输入框

## 数据模型

```dart
// categories 表
class Category {
  final int id;
  final String name;       // 分类名称
  final String iconEmoji;  // 图标emoji
}

// assets 表
class Asset {
  final int id;
  final String name;         // 资产名称
  final double price;        // 购买价格
  final String purchaseDate; // 购买日期 yyyy-MM-dd
  final String status;       // 服役中 | 已退役 | 已卖出
  final String iconEmoji;    // 图标emoji，默认 📱
  final int? categoryId;     // 所属分类ID，可为空
}
```

## 路由设计

| 路径                        | 页面         | 说明                     |
| --------------------------- | ------------ | ------------------------ |
| `/login`                    | 登录页       | 启动时认证拦截           |
| `/`                         | 总览页       | 默认首页，底部导航第一项 |
| `/stats`                    | 统计页       | 底部导航第二项           |
| `/assets`                   | 全部资产页   | 底部导航第三项           |
| `/settings`                 | 设置页       | 底部导航第四项           |
| `/assets/add`               | 添加资产页   | 从FAB按钮进入            |
| `/assets/:id/edit`          | 编辑资产页   | 从列表点击进入           |
| `/settings/categories`      | 分类管理页   | 从设置页进入             |
| `/settings/backup`          | 备份与恢复页 | 从设置页进入             |
| `/settings/change-password` | 修改密码页   | 从设置页进入             |
| `/settings/privacy-policy`  | 隐私政策页   | 从设置页进入             |
| `/settings/user-agreement`  | 用户协议页   | 从设置页进入             |

## 目录结构

```
lib/
├── main.dart                         # 应用入口
├── app.dart                          # MaterialApp 配置（主题色 + 路由）
├── router.dart                       # GoRouter 路由配置（含认证重定向）
├── database/
│   ├── database.dart                 # drift 数据库定义（Assets + Categories）
│   ├── database.g.dart               # drift 自动生成文件
│   ├── connection_io.dart            # 原生平台数据库连接
│   └── connection_web.dart           # Web平台数据库连接（WASM）
├── providers/
│   ├── asset_provider.dart           # 资产数据 Provider
│   ├── settings_provider.dart        # 设置状态 Provider（含持久化）
│   └── auth_provider.dart            # 认证状态 Provider（密码）
├── screens/
│   ├── home_screen.dart              # 主框架（4Tab底部导航 + FAB）
│   ├── overview_screen.dart          # 资产总览页
│   ├── stats_screen.dart             # 统计图表页（含资产趋势图）
│   ├── all_assets_screen.dart        # 全部资产页
│   ├── settings_screen.dart          # 设置页（含安全与隐私区域）
│   ├── login_screen.dart             # 登录/设置密码页（动画）
│   ├── change_password_screen.dart   # 修改密码页
│   ├── add_edit_asset_screen.dart    # 添加/编辑资产页
│   ├── category_manager_screen.dart  # 分类管理页
│   ├── backup_restore_screen.dart    # 备份与恢复页（含CSV导出）
│   ├── privacy_policy_screen.dart    # 隐私政策页
│   └── user_agreement_screen.dart    # 用户协议页
├── widgets/
│   ├── stat_card.dart                # 统计卡片组件
│   ├── asset_list_item.dart          # 资产列表项组件
│   ├── asset_summary_card.dart       # 资产摘要卡片
│   └── empty_state.dart              # 公共空状态组件
└── utils/
    ├── calculator.dart               # 天数、日均成本、金额格式化
    └── seed_data.dart                # 12条模拟数据
```

## 关键依赖

```yaml
# pubspec.yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.0
  riverpod_annotation: ^2.3.0
  go_router: ^14.0.0
  drift: ^2.16.0
  sqlite3_flutter_libs: ^0.5.0
  sqlite3_web: ^0.4.0
  sqlite3: ^2.9.0
  path_provider: ^2.1.0
  path: ^1.9.0
  intl: ^0.19.0
  shared_preferences: ^2.5.0
  package_info_plus: ^9.0.0
  flutter_secure_storage: ^9.2.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  drift_dev: ^2.16.0
  build_runner: ^2.4.0
  riverpod_generator: ^2.4.0
  custom_lint: ^0.6.0
  riverpod_lint: ^2.3.0
```

## Android 12.0 (API 31) 适配要点

1. **android/app/build.gradle.kts 配置**：
   - `compileSdk` 设为 34+
   - `minSdk` 设为 31
   - `targetSdk` 设为 34+

2. **通知权限**：若后续添加通知功能，需在 `AndroidManifest.xml` 中声明 `POST_NOTIFICATIONS` 权限，并在运行时动态请求。

3. **导出组件声明**：所有包含 `<intent-filter>` 的 Activity/Service/Receiver 必须显式声明 `android:exported`。

4. **PendingIntent 可变性**：创建 PendingIntent 时必须指定 `FLAG_IMMUTABLE` 或 `FLAG_MUTABLE`。

5. **隐私沙盒**：无需特殊处理，drift 数据库存储在应用私有目录，符合隐私要求。

6. **返回手势适配**：在 `AndroidManifest.xml` 中启用 `android:enableOnBackInvokedCallback="true"`，支持 Android 13+ 预测性返回手势。

7. **启动闪屏**：通过 `launch_background.xml` 配置应用图标闪屏，避免启动白屏。

## 开发环境搭建步骤

```bash
# 1. 创建 Flutter 项目
flutter create --org com.example mymastery
cd mymastery

# 2. 添加依赖
flutter pub add flutter_riverpod riverpod_annotation go_router drift sqlite3_flutter_libs sqlite3_web sqlite3 path_provider path intl
flutter pub add -d drift_dev build_runner riverpod_generator custom_lint riverpod_lint flutter_lints

# 3. 生成 drift 代码
dart run build_runner build

# 4. 运行项目
flutter run
```

## 核心计算逻辑

```dart
/// 计算已使用天数
int calculateDaysUsed(String purchaseDate) {
  final purchase = DateTime.parse(purchaseDate);
  final today = DateTime.now();
  return today.difference(purchase).inDays;
}

/// 计算日均成本
double calculateDailyCost(double price, int daysUsed) {
  if (daysUsed <= 0) return price;
  return price / daysUsed;
}

/// 格式化金额（无千位分隔符）
String formatPrice(double amount) {
  return '¥${amount.toStringAsFixed(2)}';
}

/// 格式化金额（可选千位分隔符）
String formatCurrency(double amount, {bool useSeparator = false}) {
  if (useSeparator) {
    final formatter = NumberFormat('#,##0.00');
    return '¥${formatter.format(amount)}';
  }
  return formatPrice(amount);
}
```

## 状态管理设计（Riverpod）

```dart
// 数据库 Provider
final databaseProvider = Provider<AppDatabase>((ref) {
  final db = AppDatabase();
  ref.onDispose(() => db.close());
  return db;
});

// 资产列表 Provider
final assetListProvider = FutureProvider<List<Asset>>((ref) async {
  final db = ref.watch(databaseProvider);
  return db.getAllAssets();
});

// 设置 Provider（含持久化）
class AppSettings {
  final bool useSeparator;      // 千位分隔符
  final String durationFormat;  // 天数 | 日期
  final AppThemeMode themeMode; // 主题模式
  final String currency;        // 货币单位
  final int decimalPlaces;      // 小数位数
}

final settingsProvider =
    AsyncNotifierProvider<SettingsNotifier, AppSettings>(SettingsNotifier.new);

// 认证 Provider
enum AuthStatus { unknown, locked, unlocked, noPassword }

final authProvider =
    AsyncNotifierProvider<AuthNotifier, AuthStatus>(AuthNotifier.new);
// 支持：设置密码、验证密码、修改密码、锁定/解锁
```

## 认证流程

```
启动 App
  ↓
检查 authProvider 状态
  ├─ noPassword → 显示设置密码界面（首次使用）
  ├─ locked     → 显示登录界面
  └─ unlocked   → 进入主界面
  ↓
登录成功后 → 状态变为 unlocked → 进入主界面
退出登录   → 状态变为 locked   → 下次启动需重新认证

平台差异：
- Web 端：默认强制登录，未登录时自动跳转到登录页
- Android 端：默认直接进入主界面，可在设置中开启「启动需解锁」
```

## Android 权限配置

`AndroidManifest.xml` 中已添加：

```xml
<uses-permission android:name="android.permission.USE_BIOMETRIC"/>
```

## Release 发布配置

### 签名打包

- 签名密钥：`android/app/mymastery-release-key.jks`（RSA 2048位，有效期10000天）
- 密钥配置：`android/key.properties`（已加入 `.gitignore`）
- 构建配置：`android/app/build.gradle.kts` 中配置 `signingConfigs.release`

### 混淆与资源压缩

- `isMinifyEnabled = true`：启用 R8 代码混淆
- `isShrinkResources = true`：启用资源压缩
- ProGuard 规则：`android/app/proguard-rules.pro`（保留 Flutter、Drift、SecureStorage 等必要类）

### App 名称与图标

- 名称：`android:label="资产"`
- 图标：通过 `flutter_launcher_icons` 自动生成多尺寸适配（mdpi ~ xxxhdpi）

## 模拟数据

| 资产                 | 价格    | 购买日期   | 状态   | 分类     |
| -------------------- | ------- | ---------- | ------ | -------- |
| iPhone 15 Pro        | ¥8,999  | 2024-09-22 | 服役中 | 电子产品 |
| MacBook Pro 14       | ¥14,999 | 2024-03-15 | 服役中 | 电子产品 |
| AirPods Pro 2        | ¥1,899  | 2024-06-10 | 服役中 | 电子产品 |
| iPad Air             | ¥4,799  | 2023-11-05 | 服役中 | 电子产品 |
| Sony PS5             | ¥3,899  | 2023-08-20 | 服役中 | 电子产品 |
| 戴森吸尘器 V15       | ¥4,990  | 2023-06-18 | 服役中 | 家居家具 |
| 小米电动滑板车       | ¥1,999  | 2022-05-01 | 已退役 | 交通工具 |
| Kindle Paperwhite    | ¥1,068  | 2021-12-12 | 已卖出 | 书籍文具 |
| Nintendo Switch      | ¥2,199  | 2022-03-08 | 已卖出 | 电子产品 |
| Apple Watch S9       | ¥3,199  | 2024-01-20 | 服役中 | 电子产品 |
| 机械键盘 HHKB        | ¥1,699  | 2023-09-15 | 服役中 | 电子产品 |
| 4K 显示器 Dell U2723 | ¥3,499  | 2023-04-22 | 服役中 | 电子产品 |

## 后续规划

- [x] 分类管理页面（增删改查）
- [x] 备份与恢复（导出/导入 JSON）
- [x] 设置持久化（shared_preferences）
- [x] 主题模式切换（亮色/暗色/跟随系统）
- [x] 货币单位与小数点设置
- [x] 版本号动态读取
- [x] 密码登录认证
- [x] 修改密码功能
- [x] 登录界面 UI 优化（动画 + 渐变）
- [x] 资产趋势图（按购买月份统计累计价值）
- [x] CSV 报告导出（系统分享面板）
- [x] 多账号切换（数据隔离，密码验证删除）
- [x] 忘记密码说明（本地密码无法恢复）
- [x] Release 签名打包（密钥配置 + build.gradle.kts）
- [x] 混淆与资源压缩（R8 + shrinkResources）
- [x] 启动闪屏（应用图标背景）
- [x] 返回手势适配（Android 13+ 预测性返回）
- [x] 隐私政策页面
- [x] 用户协议页面
- [x] Web 端支持（WASM + Drift）
- [ ] 预算提醒
- [ ] 多账本切换
- [ ] 数据加密（SQLCipher）
- [ ] 标签管理
- [ ] 反馈提意见功能
- [ ] Windows 桌面端适配

## 注意事项

- 所有文本使用中文
- 金额使用人民币格式（¥ 符号）
- 日期格式统一使用 `yyyy-MM-dd`
- 空状态需要友好提示文案
- 删除操作必须有确认弹窗
- Web 端需要 `sqlite3.wasm` 和 `drift_worker.js` 放在 `web/` 目录下
- Web 部署时服务器必须支持 `.wasm` MIME 类型（`application/wasm`）并添加跨域隔离头（`Cross-Origin-Opener-Policy: same-origin`、`Cross-Origin-Embedder-Policy: require-corp`）
