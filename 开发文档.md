# mymastery - Android Studio 开发的个人资产管理手机App

## 项目概述

一款基于 **Flutter** 框架开发的个人资产管理应用，帮助用户记录和管理购买的电子产品、家具、车辆等资产，计算日均使用成本，直观了解资产价值分布。

## 技术栈

| 类别     | 技术选型                      | 说明                       |
| -------- | ----------------------------- | -------------------------- |
| 框架     | Flutter 3.x                   | Dart 语言，跨平台UI框架    |
| 状态管理 | Riverpod 2.x                  | 响应式、类型安全的状态管理 |
| 数据库   | drift (SQLite)                | 类型安全的SQLite ORM       |
| 路由     | go_router                     | 声明式路由管理             |
| UI       | Material Design 3             | Flutter 原生 M3 支持       |
| 主题色   | 草绿色 (#4CAF50)              | 清新、资产感               |
| 最低SDK  | Android API 31 (Android 12.0) | 目标平台                   |

## 功能模块

### 1. 资产总览（首页）

- 统计卡片：总资产价值、日均使用成本、服役中数量、已退役/已卖出数量
- 最近添加的5条资产记录
- 空状态提示 + 加载示例数据按钮

### 2. 统计图表

- 资产统计概览：总价值、日均成本、资产总数
- 状态分布：服役中/已退役/已卖出 占比进度条
- 价值排行：按价格降序排列 Top 10
- 空状态提示

### 3. 全部资产

- 资产列表展示（图标、名称、价格、状态、使用天数、日均成本）
- 支持千位分隔符、时长显示格式切换（天数/日期）
- 点击资产进入编辑
- 滑动或长按删除（带确认弹窗）
- 空状态提示 + 加载示例数据按钮

### 4. 添加/编辑资产

- 选择图标（emoji列表）
- 输入资产名称
- 输入购买价格
- 选择购买日期（日期选择器）
- 选择状态：服役中 / 已退役 / 已卖出
- 选择资产分类
- 表单校验

### 5. 设置

- 数值与单位：千位分隔符开关、时长显示格式（天数/日期）
- 数据管理：分类管理、备份与恢复
- 关于：版本信息

## 数据模型

```dart
// categories 表
class Category {
  final int id;
  final String name;       // 分类名称
  final String iconEmoji;  // 图标emoji
}

// assets 表
class Asset {
  final int id;
  final String name;         // 资产名称
  final double price;        // 购买价格
  final String purchaseDate; // 购买日期 yyyy-MM-dd
  final String status;       // 服役中 | 已退役 | 已卖出
  final String iconEmoji;    // 图标emoji，默认 📱
  final int? categoryId;     // 所属分类ID，可为空
}
```

## 路由设计

| 路径                   | 页面         | 说明                     |
| ---------------------- | ------------ | ------------------------ |
| `/`                    | 总览页       | 默认首页，底部导航第一项 |
| `/stats`               | 统计页       | 底部导航第二项           |
| `/assets`              | 全部资产页   | 底部导航第三项           |
| `/settings`            | 设置页       | 底部导航第四项           |
| `/assets/add`          | 添加资产页   | 从FAB按钮进入            |
| `/assets/:id/edit`     | 编辑资产页   | 从列表点击进入           |
| `/settings/categories` | 分类管理页   | 从设置页进入             |
| `/settings/backup`     | 备份与恢复页 | 从设置页进入             |

## 目录结构

```
lib/
├── main.dart                    # 应用入口
├── app.dart                     # MaterialApp 配置（主题色）
├── router.dart                  # GoRouter 路由配置
├── database/
│   ├── database.dart            # drift 数据库定义（Assets + Categories）
│   ├── database.g.dart          # drift 自动生成文件
│   ├── connection_io.dart       # 原生平台数据库连接
│   └── connection_web.dart      # Web平台数据库连接（WASM）
├── providers/
│   ├── asset_provider.dart      # 资产数据 Provider
│   └── settings_provider.dart   # 设置状态 Provider
├── screens/
│   ├── home_screen.dart         # 主框架（4Tab底部导航 + FAB）
│   ├── overview_screen.dart     # 资产总览页
│   ├── stats_screen.dart        # 统计图表页
│   ├── all_assets_screen.dart   # 全部资产页
│   ├── settings_screen.dart     # 设置页
│   ├── add_edit_asset_screen.dart # 添加/编辑资产页
│   ├── category_manager_screen.dart # 分类管理页
│   └── backup_restore_screen.dart  # 备份与恢复页
├── widgets/
│   ├── stat_card.dart           # 统计卡片组件
│   ├── asset_list_item.dart     # 资产列表项组件
│   ├── asset_summary_card.dart  # 资产摘要卡片
│   └── empty_state.dart         # 公共空状态组件
└── utils/
    ├── calculator.dart          # 天数、日均成本、金额格式化
    └── seed_data.dart           # 12条模拟数据
```

## 关键依赖

```yaml
# pubspec.yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.0
  riverpod_annotation: ^2.3.0
  go_router: ^14.0.0
  drift: ^2.16.0
  sqlite3_flutter_libs: ^0.5.0
  sqlite3_web: ^0.4.0
  sqlite3: ^2.9.0
  path_provider: ^2.1.0
  path: ^1.9.0
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  drift_dev: ^2.16.0
  build_runner: ^2.4.0
  riverpod_generator: ^2.4.0
  custom_lint: ^0.6.0
  riverpod_lint: ^2.3.0
```

## Android 12.0 (API 31) 适配要点

1. **android/app/build.gradle 配置**：
   - `compileSdk` 设为 34+
   - `minSdk` 设为 31
   - `targetSdk` 设为 34+

2. **通知权限**：若后续添加通知功能，需在 `AndroidManifest.xml` 中声明 `POST_NOTIFICATIONS` 权限，并在运行时动态请求。

3. **导出组件声明**：所有包含 `<intent-filter>` 的 Activity/Service/Receiver 必须显式声明 `android:exported`。

4. **PendingIntent 可变性**：创建 PendingIntent 时必须指定 `FLAG_IMMUTABLE` 或 `FLAG_MUTABLE`。

5. **隐私沙盒**：无需特殊处理，drift 数据库存储在应用私有目录，符合隐私要求。

## 开发环境搭建步骤

```bash
# 1. 创建 Flutter 项目
flutter create --org com.example mymastery
cd mymastery

# 2. 添加依赖
flutter pub add flutter_riverpod riverpod_annotation go_router drift sqlite3_flutter_libs sqlite3_web sqlite3 path_provider path intl
flutter pub add -d drift_dev build_runner riverpod_generator custom_lint riverpod_lint flutter_lints

# 3. 生成 drift 代码
dart run build_runner build

# 4. 运行项目
flutter run
```

## 核心计算逻辑

```dart
/// 计算已使用天数
int calculateDaysUsed(String purchaseDate) {
  final purchase = DateTime.parse(purchaseDate);
  final today = DateTime.now();
  return today.difference(purchase).inDays;
}

/// 计算日均成本
double calculateDailyCost(double price, int daysUsed) {
  if (daysUsed <= 0) return price;
  return price / daysUsed;
}

/// 格式化金额（无千位分隔符）
String formatPrice(double amount) {
  return '¥${amount.toStringAsFixed(2)}';
}

/// 格式化金额（可选千位分隔符）
String formatCurrency(double amount, {bool useSeparator = false}) {
  if (useSeparator) {
    final formatter = NumberFormat('#,##0.00');
    return '¥${formatter.format(amount)}';
  }
  return formatPrice(amount);
}
```

## 状态管理设计（Riverpod）

```dart
// 数据库 Provider
final databaseProvider = Provider<AppDatabase>((ref) {
  final db = AppDatabase();
  ref.onDispose(() => db.close());
  return db;
});

// 资产列表 Provider
final assetListProvider = FutureProvider<List<Asset>>((ref) async {
  final db = ref.watch(databaseProvider);
  return db.getAllAssets();
});

// 设置 Provider
class AppSettings {
  final bool useSeparator;     // 千位分隔符
  final String durationFormat; // 天数 | 日期
}

final settingsProvider = StateNotifierProvider<SettingsNotifier, AppSettings>(...);
```

## 模拟数据

| 资产                 | 价格    | 购买日期   | 状态   | 分类     |
| -------------------- | ------- | ---------- | ------ | -------- |
| iPhone 15 Pro        | ¥8,999  | 2024-09-22 | 服役中 | 电子产品 |
| MacBook Pro 14       | ¥14,999 | 2024-03-15 | 服役中 | 电子产品 |
| AirPods Pro 2        | ¥1,899  | 2024-06-10 | 服役中 | 电子产品 |
| iPad Air             | ¥4,799  | 2023-11-05 | 服役中 | 电子产品 |
| Sony PS5             | ¥3,899  | 2023-08-20 | 服役中 | 电子产品 |
| 戴森吸尘器 V15       | ¥4,990  | 2023-06-18 | 服役中 | 家居家具 |
| 小米电动滑板车       | ¥1,999  | 2022-05-01 | 已退役 | 交通工具 |
| Kindle Paperwhite    | ¥1,068  | 2021-12-12 | 已卖出 | 书籍文具 |
| Nintendo Switch      | ¥2,199  | 2022-03-08 | 已卖出 | 电子产品 |
| Apple Watch S9       | ¥3,199  | 2024-01-20 | 服役中 | 电子产品 |
| 机械键盘 HHKB        | ¥1,699  | 2023-09-15 | 服役中 | 电子产品 |
| 4K 显示器 Dell U2723 | ¥3,499  | 2023-04-22 | 服役中 | 电子产品 |

## 后续规划

- [x] 分类管理页面（增删改查）
- [x] 备份与恢复（导出/导入 JSON）
- [ ] 深色模式
- [ ] 预算提醒
- [ ] 多账本切换
- [ ] 数据加密

## 注意事项

- 所有文本使用中文
- 金额使用人民币格式（¥ 符号）
- 日期格式统一使用 `yyyy-MM-dd`
- 空状态需要友好提示文案
- 删除操作必须有确认弹窗
- Web 端需要 `sqlite3.wasm` 和 `drift_worker.js` 放在 `web/` 目录下
